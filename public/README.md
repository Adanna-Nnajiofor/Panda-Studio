Place demo assets here for the homepage demo modal.

- `demo.mp4` — optional short demo video (recommended h.264 or webm, under ~10MB for quick loading). Replace the placeholder `demo.mp4` file with a real video.
- `demo-poster.svg` — poster image shown before playback (lightweight SVG provided).

Behavior:

- The modal shows the poster with a play overlay. Clicking play loads and plays `demo.mp4`.
- If the video can't be loaded, the modal falls back to the animated mockup.

Notes:

- You can replace `demo-poster.svg` with a static `demo-poster.jpg` or `demo-poster.png` if preferred.
- Keep the demo short (10–20s) for best UX.
